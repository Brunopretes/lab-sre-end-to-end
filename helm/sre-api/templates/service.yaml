apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sre-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

